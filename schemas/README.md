# Schemas

Shared schema files will live here once the first mobile workflow needs
cross-platform fixtures or generated bindings.

Expected contents:

- UniFFI interface definitions
- JSON fixtures
- protocol buffers, if mobile needs direct service messages
- schema version notes

